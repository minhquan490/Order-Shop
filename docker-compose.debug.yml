version: '3.4'

services:
  ordershoprefactor:
    image: ordershoprefactor
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      JAVA_OPTS: -agentlib:jdwp=transport=dt_socket,address=127.0.0.1:45231,suspend=y,server=n